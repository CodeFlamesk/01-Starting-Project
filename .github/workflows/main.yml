name: First Workflow
on: workflow_dispatch
jobs:
 first-jobs:
      runs-on: ubuntu-latest
      steps:
      - name: print greeting
        run: echo "Hello World!" 
      - name: Print goodbye
        run: echo "Done - bye!"
